"""The benchmark suite: a hermetic tier, and a tier over real repositories.

Nothing third-party is committed under `benchmarks/` — `fixtures/` is ours and
`repos/` stays empty until `benchmarks.vendor` is run deliberately. See
docs/08-benchmarks.md.
"""
