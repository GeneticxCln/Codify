"""Runnable tools that are not part of the engine.

A package only so the engine's tests can import them (`scripts.replay_trace`)
and the type checker can follow that import; each file still runs directly
from a shell.
"""
